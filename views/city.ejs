<!DOCTYPE html>
<html>
<% include partials/head.ejs %>
<body>
    <% include partials/menu.ejs %>
    <% include partials/headline.ejs %>
    <div class="container">
        <div class="row">
            <% for(var i = 1; i <= imageCount; i++) { %>
            <div class="col-md-6">
                <div class="thumbnail">
                    <img src="/images/<%= cityName %>/<%= cityName %>-<%= i %>.jpg" class="img-responsive" alt="iLoveMyCity"/>
                </div>
            </div>
            <% } //end for%>
        </div>
    </div>
    <% include partials/jsincludes.ejs %>
</body>
</html>